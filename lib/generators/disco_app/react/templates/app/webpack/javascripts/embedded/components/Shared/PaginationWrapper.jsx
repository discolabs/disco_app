import React from 'react';
import { Pagination } from '@shopify/polaris';

const PaginationWrapper = props => (
  <div className="Disco-PaginationWrapper">
    <Pagination {...props} />
  </div>
);

export default PaginationWrapper;
